1. use wget to download the torrent files, 2 per group
   a group are those whose url identical less a trailing numeric or datetime index
2. read the torrent file to find the filenames contained in it
3. for each group of torrent file, sample some filenames and store in an yaml to show filename convention