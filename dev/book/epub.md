write a light-weight epub loader (w/o using e.g. Ebooklib):
    loads an epub file of a book into a common structure, 
    where each chapter is a Chapter object and Book object is a collection of Chapters.

    only text is retained. 